load('signal_lab1.mat')
fs=1000
len=length(signal1);

n=0:len-1;
N=n*fs/(len-1); % convert x-axis in actual frequency

y=fft(signal1);
plot(N,abs(y));

ans = 0:(len)/100-1 ;
sample_point=10;
while sample_value <length(ans)
    sample_value+=10
end