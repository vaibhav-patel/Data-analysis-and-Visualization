load('signal_lab1.mat')
fs = 1000
len = length(signal2);
ans = zeros(len);
sample_frequency = 100;
sample_points = randperm(fs, sample_frequency);
sample_points = sort(sample_points);
sampled_signal = signal2(sample_points);
xq=1:1:len;
yq=
cost_func = 'NRMSE';
y = y_sim.y;
fit = goodnessOfFit(y,yref,cost_func);
plot(sample_points,sampled_signal);
hold on;
plot(signal2)